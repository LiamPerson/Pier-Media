import { Paper } from '@mui/material'
import { SIDEBAR_WIDTH } from './constants'

const Sidebar = () => <Paper sx={{ position: 'absolute', top: 0, left: 0, bottom: 0, width: SIDEBAR_WIDTH }}>side</Paper>

export default Sidebar
